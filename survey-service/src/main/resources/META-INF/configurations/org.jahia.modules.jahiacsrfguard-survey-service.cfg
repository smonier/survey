# CSRF Guard whitelist for Survey Service actions
# Allows the browser JS to POST to the surveyAnalysisAction endpoint
# without a CSRF token (session-cookie auth is sufficient in the back-office)
whitelist = *.surveyAnalysisAction.do, *.saveAnalysisAction.do
